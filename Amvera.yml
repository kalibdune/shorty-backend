meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
  skip: false
run:
  image: kalibdune/shorty-backend:latest
  command: alembic upgrade head && python -m shorty
  args: null
  persistenceMount: /data
  containerPort: "8000"
